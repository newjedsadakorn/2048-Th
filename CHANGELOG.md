# บันทึกการเปลี่ยนแปลง

รูปแบบเวอร์ชันตาม [Semantic Versioning](https://semver.org/lang/th/)

## [1.2.0]

### เพิ่ม
- ระบบมุมโค้งสี่ระดับแยกกัน (`--rc` `--ru` `--rs` `--rt`) พร้อมระยะขอบที่โตตามมุมโค้ง
- กรอบจางบนทุกกล่อง เพื่อแยกกล่องจากพื้นหลังโดยไม่เห็นเป็นเส้น
- ชุดสีใหม่สามชุด พลาสมา น้ำแข็ง ดินเผา รวมชุดที่ปลดล็อกได้เป็น 6 ชุด
- ของรางวัลเช็คอินหมุนใหม่ทุกเดือน พร้อมตัวอย่างสีข้างชื่อรางวัลและเหรียญประจำเดือน
- อีโมจิไฟและการไฮไลต์แถบวันต่อเนื่องในปฏิทิน
- แสดงเวอร์ชันในเมนูและหน้าตั้งค่า

### เปลี่ยน
- ย้ายทางเข้าหน้าธีมไปอยู่ในหน้าตั้งค่า เมนูหลักเหลือสี่ช่อง
- ปฏิทินเช็คอิน เปลี่ยนน้ำหนักสี วันที่พลาดจางที่สุด วันในอนาคตเข้มกว่า
- สีวันที่เช็คแล้วเป็นเขียวสาจาง มีเวอร์ชันสำหรับธีมมืดแยกต่างหาก

### แก้
- หน้าธีมเลื่อนลงไม่ได้ เพราะ `.app` ถูกตั้งเป็น `flex:1` ในคอนเทนเนอร์ที่สูงเท่าจอ

## [1.1.0]

### เพิ่ม
- หน้ากั้นเมื่อจอเป็นแนวนอน พร้อมพยายามล็อกผ่าน `screen.orientation.lock()`
- เกมค้างไว้เมื่อออกไปหน้าอื่น และมีปุ่มเล่นต่อในเมนู

### แก้
- กระดานคะแนนรวมขึ้นว่างตลอด เพราะอ่าน `snapshot.data` เป็นพร็อพเพอร์ตี้ทั้งที่เป็นเมธอด
- คลาส `row` ถูกใช้ซ้ำระหว่างแถวกระดานคะแนนกับแถวปุ่มบนม่านจบเกม แยกเป็น `vrow`
- วันปัจจุบันบนปฏิทินถูกจัดเป็นวันที่พลาด ทำให้กรอบไฮไลต์จางจนแทบมองไม่เห็น
- วันปัจจุบันที่เช็คแล้วมีสีตัวอักษรทับกันจนอ่านไม่ออก
- กระดานล้นจอบนหน้าจอเตี้ย เพิ่มเพดานความกว้างตามความสูงจอและเปลี่ยนไปใช้ `dvh`
- เปลี่ยนความโค้งแล้วมุมของช่องว่างบนกระดานไม่เปลี่ยนตาม

## [1.0.0]

- เกม 2048 พร้อมโหมด 3×3 4×4 5×5 และท้าทายประจำวัน
- ระบบธีม เช็คอินรายวัน กระดานคะแนน สถิติ และหน้าตั้งค่า
